# Dockerizacion
App Space ivaders 

======================{ Instalacion en entorno Ubuntu }=======================

--Actualizacion del sistema--
sudo apt update  && apt upgrade -y

====( Instalaccion de Dependencias para la ejecucion del Programa )====
sudo apt install -y nodejs npm
